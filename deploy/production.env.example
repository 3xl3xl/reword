# Domain only: no scheme, path, or port. DNS must point at the deployment host.
REWORD_DOMAIN=reword.example.com
ACME_EMAIL=operator@example.com
# Exact values from your identity provider, which must support the token profile in README.
OAUTH_ISSUER=https://identity.example.com/
OAUTH_JWKS_URL=https://identity.example.com/jwks
